# .gitignore for Tango

# Environment & secrets
.env
.env.*
!.env.example

# Python
# Global patterns, not per-directory ones. This listed tests/ and
# src/pipeline/ by name, so scripts/__pycache__ was never covered and a .pyc
# was committed. Anchoring to directory names catches every directory,
# including ones that do not exist yet.
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
src/*.egg-info/
*.egg
dist/
build/
.eggs/

# Virtual environment
.tangovenv/
.venv/
venv/
env/

# Pipeline output
output/
*.apkg
pipeline.db
# SQLite writes these two alongside the database in WAL mode, which
# _get_db() enables. They are transient and meaningless without the .db.
pipeline.db-shm
pipeline.db-wal
review.json

# Offline Wiktionary indexes: hundreds of MB each, rebuildable at any time
# with `make dictionary LANGUAGE=<code>`. Never commit these.
dictionaries/

# Intermediates from scripts/measure_*.py. Kept out of dictionaries/ on
# purpose: that directory's size is a release metric.
measurements/

# Logs
logs/
*.log
*.jsonl

# Testing & coverage
.pytest_cache/
.coverage
htmlcov/
coverage.xml

# Type checking
.mypy_cache/

# Linting
.ruff_cache/

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# spaCy model cache
# Models are downloaded via 'make spacy-model', not committed
*.tar.gz

# Claude Settings
.claude/settings.local.json
# Pronunciation audio cache (ADR-009). Rebuildable, and large in aggregate.
media/

# Written by scripts/pypi_readme.py --pin while a release is being built,
# and removed by --restore. Present only if a release was interrupted.
README.md.orig

# Card image cache (ADR-009 phase 3). Same reasoning as media/: rebuildable,
# and it grows with every deck. Note this is the cache at the repo root, not
# docs/assets/, which holds committed diagrams.
images/

# Third-party agent skills, installed per-machine by `npx skills add`.
# Not this project's code, and they run with full agent permissions, so
# they stay out of the repository rather than being shipped to anyone who
# clones it.
.agents/
.claude/skills/
skills-lock.json
